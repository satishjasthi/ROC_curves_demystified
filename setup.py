from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Project to understand what  exactly ROC curve is and who to use it to evaluate classification model',
    author='Satish Jasthi',
    license='MIT',
)
